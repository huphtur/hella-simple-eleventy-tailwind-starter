{
  "name": "hella-simple-eleventy-tailwind-starter",
  "description": "Hella Simple Eleventy + Tailwind Starter",
  "scripts": {
    "dev": "rm -rf _site && eleventy --serve --config=eleventy.config.js & NODE_ENV=development postcss tailwind.css --o src/_includes/output.css --watch",
    "build": "rm -rf _site && NODE_ENV=production postcss tailwind.css --o src/_includes/output.css && ELEVENTY_ENV=production eleventy --config=eleventy.config.js"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.10",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.1.1"
  }
}
